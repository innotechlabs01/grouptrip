# Local databases (Turso/libSQL runtime + test artifacts)
*.db
*.db-wal
*.db-shm
*.db-journal

# Secrets / local env (keep tokens out of git)
.env
.env.local
.env.*.local
!.env.example
!.env.*.example

# Go
backend/*.test

# PWA (once scaffolded)
pwa/node_modules/
pwa/.next/
pwa/out/

# OS / editor
.DS_Store
